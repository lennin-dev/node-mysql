<h2>Editar Livro</h2>

<form action="/edit/save" method="POST">
<input type="hidden" value="{{book.id}}" name="id">
<div>
    <label for="">Titulo:</label>
    <input type="text" name="title" placeholder="Digite o titulo: " id="title" value="{{book.title}}">
</div>

<div>
    <label for="">Número de Páginas:</label>
    <input type="number" name="pageqty" placeholder="Quantas páginas tem o livro?" id="pageqty" value="{{book.pageqty}}">
</div>

<button type="submit">Enviar</button>

</form>